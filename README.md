# ros2_decoding_package
repo for ros2 package used to decode image topic

build and source the command before running the following ommand to start the node.

```ros2 run yuv422_to_rgb yuv422_to_rgb_node```